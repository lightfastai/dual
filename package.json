{
  "name": "dual-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo for dual CLI and npm wrapper",
  "scripts": {
    "changeset": "changeset",
    "changeset:add": "changeset",
    "version": "changeset version",
    "release": "changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.0"
  },
  "workspaces": [
    "npm"
  ]
}
